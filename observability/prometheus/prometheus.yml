global:
  scrape_interval: 5s
  evaluation_interval: 5s

scrape_configs:
  - job_name: driver-service
    metrics_path: /metrics/prometheus
    static_configs:
      - targets:
          - driver-service:3003
